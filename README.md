# team-context branch

This branch is auto-managed by the Handoff tool. It carries shared-context
data (one folder per teammate, one subfolder per branch) consumed by the
Claude Code / Codex injection hook.

Do not edit by hand. The daemon under app/Handoff.WinUI/ owns writes.