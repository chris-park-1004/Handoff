<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="Handoff.WinUI.Pages.ReceiverHostWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="Handoff">
    <Grid x:Name="RootGrid" Background="{ThemeResource ApplicationPageBackgroundThemeBrush}" Opacity="0" Loaded="OnRootLoaded">
        <Grid.RenderTransform>
            <CompositeTransform x:Name="RootTransform" ScaleX="0.96" ScaleY="0.96" />
        </Grid.RenderTransform>
        <Grid.RenderTransformOrigin>0.5,0.5</Grid.RenderTransformOrigin>
        <Grid.Resources>
            <Storyboard x:Name="EnterStoryboard">
                <DoubleAnimation Storyboard.TargetName="RootGrid" Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:0.28">
                    <DoubleAnimation.EasingFunction><CubicEase EasingMode="EaseOut" /></DoubleAnimation.EasingFunction>
                </DoubleAnimation>
                <DoubleAnimation Storyboard.TargetName="RootTransform" Storyboard.TargetProperty="ScaleX" From="0.96" To="1" Duration="0:0:0.32">
                    <DoubleAnimation.EasingFunction><CubicEase EasingMode="EaseOut" /></DoubleAnimation.EasingFunction>
                </DoubleAnimation>
                <DoubleAnimation Storyboard.TargetName="RootTransform" Storyboard.TargetProperty="ScaleY" From="0.96" To="1" Duration="0:0:0.32">
                    <DoubleAnimation.EasingFunction><CubicEase EasingMode="EaseOut" /></DoubleAnimation.EasingFunction>
                </DoubleAnimation>
            </Storyboard>
        </Grid.Resources>
        <Grid.RowDefinitions>
            <RowDefinition Height="32" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid x:Name="DragArea" Grid.Row="0" Background="Transparent" />

        <Grid Grid.Row="1" Padding="24,8,24,24" RowSpacing="12">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <TextBlock
                Grid.Row="0"
                x:Name="HeadingText"
                Text="New team update"
                FontSize="20"
                FontWeight="SemiBold" />

            <TextBlock
                Grid.Row="1"
                x:Name="AttributionText"
                Opacity="0.75"
                TextWrapping="Wrap" />

            <TextBlock
                Grid.Row="2"
                x:Name="CommitMessageText"
                FontWeight="SemiBold"
                TextWrapping="Wrap" />

            <ScrollViewer
                Grid.Row="3"
                VerticalScrollBarVisibility="Auto"
                HorizontalScrollBarVisibility="Disabled"
                Padding="0,4,8,4">
                <TextBlock
                    x:Name="SummaryText"
                    TextWrapping="Wrap"
                    IsTextSelectionEnabled="True" />
            </ScrollViewer>

            <TextBlock
                Grid.Row="4"
                x:Name="ChangedFilesText"
                Opacity="0.7"
                FontSize="12"
                TextWrapping="Wrap" />

            <StackPanel
                Grid.Row="5"
                Orientation="Horizontal"
                HorizontalAlignment="Right"
                Spacing="8">
                <Button x:Name="DenyButton" Content="Deny" Click="OnDenyClick" MinWidth="96" />
                <Button x:Name="AcceptButton" Content="Accept" Click="OnAcceptClick" Style="{ThemeResource AccentButtonStyle}" MinWidth="96" />
            </StackPanel>
        </Grid>
    </Grid>
</Window>
