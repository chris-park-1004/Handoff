<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="Handoff.Sender.SenderHostWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="Handoff">
    <Grid x:Name="RootGrid" Background="{ThemeResource ApplicationPageBackgroundThemeBrush}" Opacity="0" Loaded="OnRootLoaded">
        <Grid.RenderTransform>
            <CompositeTransform x:Name="RootTransform" ScaleX="0.96" ScaleY="0.96" />
        </Grid.RenderTransform>
        <Grid.RenderTransformOrigin>0.5,0.5</Grid.RenderTransformOrigin>
        <Grid.Resources>
            <Storyboard x:Name="EnterStoryboard">
                <DoubleAnimation
                    Storyboard.TargetName="RootGrid"
                    Storyboard.TargetProperty="Opacity"
                    From="0" To="1" Duration="0:0:0.28">
                    <DoubleAnimation.EasingFunction>
                        <CubicEase EasingMode="EaseOut" />
                    </DoubleAnimation.EasingFunction>
                </DoubleAnimation>
                <DoubleAnimation
                    Storyboard.TargetName="RootTransform"
                    Storyboard.TargetProperty="ScaleX"
                    From="0.96" To="1" Duration="0:0:0.32">
                    <DoubleAnimation.EasingFunction>
                        <CubicEase EasingMode="EaseOut" />
                    </DoubleAnimation.EasingFunction>
                </DoubleAnimation>
                <DoubleAnimation
                    Storyboard.TargetName="RootTransform"
                    Storyboard.TargetProperty="ScaleY"
                    From="0.96" To="1" Duration="0:0:0.32">
                    <DoubleAnimation.EasingFunction>
                        <CubicEase EasingMode="EaseOut" />
                    </DoubleAnimation.EasingFunction>
                </DoubleAnimation>
            </Storyboard>
        </Grid.Resources>
        <Grid.RowDefinitions>
            <RowDefinition Height="32" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid x:Name="DragArea" Grid.Row="0" Background="Transparent" />

        <Grid Grid.Row="1" Padding="24,8,24,24" RowSpacing="12">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <TextBlock
                Grid.Row="0"
                Text="Share an update with your team"
                FontSize="20"
                FontWeight="SemiBold" />

            <TextBlock
                Grid.Row="1"
                Text="Write a quick summary of what changed. Your teammates will see this in their notification."
                TextWrapping="Wrap"
                Opacity="0.8" />

            <TextBox
                x:Name="SummaryTextBox"
                Grid.Row="2"
                PlaceholderText="e.g. Refactored the login flow to use the new auth service..."
                AcceptsReturn="True"
                TextWrapping="Wrap"
                ScrollViewer.VerticalScrollBarVisibility="Auto" />

            <Grid Grid.Row="3">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <Button
                    x:Name="AutoGenerateButton"
                    Grid.Column="0"
                    HorizontalAlignment="Left"
                    Click="OnAutoGenerateClick">
                    <StackPanel Orientation="Horizontal" Spacing="6">
                        <FontIcon FontSize="14" Glyph="&#xE945;" />
                        <TextBlock Text="Auto-generate" />
                    </StackPanel>
                </Button>

                <StackPanel
                    Grid.Column="1"
                    Orientation="Horizontal"
                    Spacing="8">
                    <Button x:Name="CancelButton" Content="Cancel" Click="OnCancelClick" MinWidth="96" />
                    <Button x:Name="SendButton" Content="Send" Click="OnSendClick" Style="{ThemeResource AccentButtonStyle}" MinWidth="96" />
                </StackPanel>
            </Grid>
        </Grid>
    </Grid>
</Window>
